fix(details): show correct data source in hero and reporting agency

The hero's "Source data" label and the Reporting Agency field were hardcoded to
PHIVOLCS/preferred API, so USGS events still displayed PHIVOLCS. Derive both from
the normalized event's `source` field (falling back to the preferred API).